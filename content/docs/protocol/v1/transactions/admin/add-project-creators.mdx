---
title: "Add Project Creators"
description: "Instance admin adds new project creators to the platform"
tx_file: "admin/add-project-creators.yaml"
---

# Add Project Creators Transaction


## Overview

This transaction allows an instance admin to add new project creators to the platform. It requires multi-signature approval from both the instance admin and global admin.

## Protocol Description

The transaction updates the global state to include new project creators in the authorized creator list. The admin state UTxO is consumed and recreated to maintain the admin's permissions, while the global data UTxO is updated with the new creator list.