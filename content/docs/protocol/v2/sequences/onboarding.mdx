---
title: "Onboarding"
description: "The single transaction every Andamio participant runs first — minting an access token."
full: true
---

import { onboarding } from "@/components/protocol/sequences/onboarding";

Every Andamio journey starts here. Before you can create or join a course or project, you mint an access token tied to a unique alias — your on-chain identity for the protocol.

<TransactionStepper title={onboarding.title} intro={onboarding.intro} steps={onboarding.steps} />
