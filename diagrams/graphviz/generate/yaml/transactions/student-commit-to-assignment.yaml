name: StudentCommitToAssignmentTransaction

# Metadata about this transaction type
metadata:
  role: "Student"
  category: "assignment_management"
  requires_tokens: ["100at", "222at"]
  estimated_fee: "2.5 ADA"
  description: "Student commits to working on a specific module assignment"

graph_attrs:
  rankdir: LR
  bgcolor: "#ffffff"
  splines: ortho
  nodesep: 0.4
  ranksep: 0.8

# Reference inputs (gray boxes on left)
references:
  - id: params_ref
    title: "params ref"
  
  - id: instance_ref  
    title: "instance ref"
    
  - id: global_state_ref
    title: "global state"

# Input UTxOs
inputs:
  - id: input_student_wallet
    title: "Student Wallet"
    address: "string"
    values:
      - "222at token"
      - "5.0 ADA"

  - id: input_course_state
    title: "Course State UTxO"
    address: "course_state_scripts"
    values:
      - "1 localStateToken"
    datum:
      assignments: "assignment_list[]"
      ls_cs: "course_state_cs"
      at_cs: "access_token_cs"
      alias: "student_alias"
    redeemer:
      id: course_state_redeemer
      title: "Commit Assignment"
      params:
        - "moduleIndex"
        - "assignmentIndex"
        - "alias"

# Reference Input UTxOs (read-only)
reference_inputs:
  - id: input_global_data
    title: "Global Data UTxO"
    address: "string"
    values:
      - "1 prt"
    datum:
      courseInstance: "(irt, course_instance)[]"
      coursePolicy: "(prt, course_policy)[]"

  - id: input_course_instance
    title: "CourseInstance UTxO"
    address: "string"
    values:
      - "1 irt"
    datum:
      ls_cs: "localStateTokens[]"
      course_nft_cs: "course_nft_cs"
      modules: "module_list[]"

  - id: input_module_ref
    title: "Module Reference UTxO"
    address: "string"
    values:
      - "1 moduleToken"
    datum:
      module_info: "module_data"
      assignments: "assignment_list[]"
      creator_policy: "creator_policy_id"

# Transaction
transaction:
  id: commit_operation
  title: "commit to\nassignment"
    
# Output UTxOs
outputs:
  - id: output_course_state
    title: "Updated Course State"
    address: "course_state_scripts"
    values:
      - "1 localStateToken"
    datum:
      assignments: "updated_assignment_list[]"
      ls_cs: "course_state_cs"
      at_cs: "access_token_cs"
      alias: "student_alias"

  - id: output_student_wallet
    title: "Student Wallet"
    address: "string"
    values:
      - "222at token"
      - "2.5 ADA"

# Automatic connections based on structure
connections:
  # Reference connections (no arrows)
  - from: params_ref
    to: input_global_data
    attrs: {dir: "none", style: "dashed"}
  
  - from: instance_ref
    to: input_course_instance  
    attrs: {dir: "none", style: "dashed"}
    
  - from: global_state_ref
    to: input_course_state
    attrs: {dir: "none", style: "dashed"}